<%= form_for @todo do |f| %>
  <p>title</p>
  <p><%= f.text_field :title %></p>
  <p>Order</p>
  <p><%= f.text_field :order %></p>
  <p><%= f.hidden_field :user_id, value: current_user.id%></p>
  <%= f.submit %>
<% end %>
