<div class="container">
  <div>
    <%= form_for @todo do |f| %>
      <h4>Title</h4>
      <%= f.text_field :title, placeholder:"title of todo..."%>
      <%= f.hidden_field :user_id, value: current_user.id %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
